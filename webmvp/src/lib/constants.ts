/** Max songs shown on Home when not searching (full library uses search). */
export const LIBRARY_BROWSE_CAP = 100;

/** Max published band playlists loaded on the Playlists page (most recent first). */
export const PUBLISHED_PLAYLIST_CAP = 100;
